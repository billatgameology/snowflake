# Phase 9's M-GT frozen foundation pins the raw bytes of these modules (see
# research/.gitattributes for why these rules cannot live in the root file: the Phase 8 freeze
# pins the root file's exact bytes).
lattice.ts -text
libbrecht.ts -text
metrics.ts -text

# Phase 10 reopens the remaining exact pre-observation radial production-side closure from S5.
checkpoint.ts -text
lk-resume-checkpoint.ts -text
params.ts -text
prng.ts -text
state.ts -text
timeline.ts -text
