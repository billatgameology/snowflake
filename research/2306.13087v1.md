# arXiv:2306.13087v1 — "A Taxonomy of Snow Crystal Growth Behaviors: 2. Quantifying the Nakaya Diagram"

K. G. Libbrecht, Caltech. Obtained 2026-07-28 at maker direction.

Media is not versioned (decision 0004). This index is the tracked artifact: it carries the source
URL, size and hash so the local copy is re-downloadable and re-verifiable, plus every extract this
project relies on, quoted rather than paraphrased.

| field | value |
|---|---|
| source | `https://arxiv.org/pdf/2306.13087v1` |
| local | `research/2306.13087v1.pdf` (gitignored) |
| bytes | 12,317,042 |
| sha256 | `20f579e01777d51b81b527751b32c3e44b1d8ebe9f1d09a7f15554c2445381af` |
| extraction | `pdftotext` (prose) and `pypdf` (the math, whose symbol font `pdftotext` drops) |

## Why it was obtained

A literature sweep reported that this paper prints closed forms related to curves
`docs/libbrecht-parameters.md` records as figure-only digitizations. More precisely, it prints the
distinct M1/M2 model prescriptions: the broad basal form is shared with the later CAK expression,
while the prism form and prefactor treatment are not the historical digitized CAK set. The maker
also directed assessment of its data set as a Phase 6 target. Both are addressed below; neither is
an automatic replacement for the frozen table or current seed geometry.

## 1. The data set is 206 PHOTOGRAPHS, not a table — and its seed is a needle

> "This paper presents a matrix of 206 snow crystal growth observations as a function of
> temperature and water vapor supersaturation in air, each illustrating the morphology and size
> of a crystal forming on the tip of **an isolated c-axis ice needle** after a known growth time."

Stated uncertainties:

> "We estimate that the temperature uncertainties in these data are typically ±0.2 C, and that the
> water-vapor supersaturations are **between 0.8 and 1.2 times the stated values**."

Two consequences for using it as a Phase 6 target, both structural rather than incidental:

- **The observations live in Figure 2, a photographic matrix.** Growth times and image scales are
  printed per panel, but there is no numeric table of `(T, σ, habit)`. Turning it into a scoring
  target means digitizing 206 panels into morphology labels — a WP1-scale digitization with its
  own read uncertainty, on images whose 3-D structure Libbrecht himself says "takes some
  experience to understand… from the 2D image projections".
- **Every crystal grows on a c-axis needle tip.** Our registered seed is the 19-site hexagonal
  plate. A sparse historical probe measured a warm `AR` shift of +57% under its sampled seed change;
  it did not rank every Phase 6 systematic. Scoring our plate-seeded runs against needle-seeded observations
  would be comparing different experiments. Matching it requires the column-seed change the plan
  already flags as ADR-level and which gates 111 of 122 entries in the project's local candidate
  index (not 111 of this source's 206 observations). The paper
  is also the source of M1's Nakaya-informed dip placement, so these panels are in-sample for M1,
  not held-out validation.

Libbrecht's own framing of the bar:

> "At present, it is not clear that any existing 3D computational models of snow crystal growth
> can adequately reproduce **any** of the structures seen in Figure 2 under prescribed growth
> conditions, let alone reproducing the entire set with even modest fidelity."

## 2. It prints CLOSED FORMS for σ₀(T) — including M2's broad-facet branch

This is the find. `α(T, σ_surf) = exp(−σ₀/σ_surf)` (Eq. 6), with **`A = 1` for every facet**:

> "To keep M1 relatively simple, we chose to set `A = 1` in Equation 3 for all growth conditions"

**M1 — with the SDAK dips**, σ₀ in **percent**, `T` the magnitude in °C, and the unstated
`log` interpreted by this project as base 10 under a P4 transcription choice because that reading
matches Figure 1's plotted dip widths. The in-sample equality count is not an independent base selector:

```
σ₀,basal(T) = (0.02·T^1.75 + 0.3) · (1 − 0.87·exp(−(log T − log 4.5)² / 0.07))
σ₀,prism(T) = (0.015·T²    + 0.02·T^0.6) · (1 − 0.95·exp(−(log T − log 14.4)² / 0.06))
```

**M2 — broad facets, the SAME curves WITHOUT the dips.** ~~This is exactly our no-SDAK case:~~

> **CORRECTION (2026-07-28).** The struck sentence is wrong, and consequentially so. M2 is the
> *width-dependent* model, not a no-SDAK model: p7 defines it as "an attachment kinetics that
> depend on the widths of the respective faceted surfaces". The undipped curves below are M2's
> **broad-facet branch only** — narrow facets keep the dipped kinetics, because M1's dipped curves
> *are* the narrow-facet kinetics ("it is a reasonable first approximation to assume that all
> faceted surfaces are described by the attachment kinetics on narrow facets", p7). And the broad
> source hypothesizes that the broad branch may be short-lived for some growth in air: "the
> Edge-Sharpening Instability (ESI) … is quite efficient in air, quickly turning broad facets into
> narrow facets during growth" (p7). The project has not established that ESI universally removes
> the broad branch. A sensitivity to these curves is a model-branch comparison. See
> [`libbrecht-figure-findings.md`](libbrecht-figure-findings.md) §4.

```
σ₀,basal(T) = 0.02·T^1.75 + 0.3
σ₀,prism(T) = 0.015·T²    + 0.02·T^0.6
```

> "While these functional forms are completely ad hoc, the values of σ₀(T) derive mainly from
> experimental data."

## 3. What the closed forms say about our digitized curves, and about SDAK

Computed, not asserted. The historical scratchpad is absent; the maintained reproducible comparison
is [`../app/scripts/phase6-libbrecht-closed-forms.mjs`](../app/scripts/phase6-libbrecht-closed-forms.mjs):

**M2 against our digitized anchors** — ratio closed-form / ours:

| T (°C) | −2 | −3 | −5 | −8 | −10 | −15 | −20 | −30 | −40 |
|---|---|---|---|---|---|---|---|---|---|
| basal | 1.05 | 0.97 | 0.91 | 0.95 | 1.02 | 1.08 | 1.07 | 1.14 | 1.17 |
| prism | **3.23** | **2.48** | **1.58** | 1.25 | 1.13 | 1.09 | 1.02 | 1.05 | 1.12 |

> # ⚠ CORRECTION — this paragraph replaces the struck historical claim below (2026-07-29)
>
> The dedicated source-analysis papers infer/source-fit σ₀,prism = **0.03%** at −2 °C
> (`2004.06212v1`) and **≈0.2%** at −5 °C (`1912.03230v1`). This project's digitized anchors are
> 0.028% at −2 °C (6.7% low) and 0.27% at −5 °C (35% high; 0.07 percentage point absolute).
> M2 is ×3.01/×2.14 and `2009.08404v2` Eq. (3) is ×1.07/×1.54 relative to those −2/−5 °C
> source fits. The former uniform 1.6–3.2×-low claim used the wrong comparator, but the corrected
> values do not support uniform ~7% agreement either. This is not
> an independent measurement of σ₀. Nothing warm-end "rests on a
> σ₀_prism 2–3× too low"; the comparison was against the wrong closed form. See
> `libbrecht-figure-findings.md` §3.

~~**Basal agrees within ~17% everywhere.** Prism agrees from −8 °C colder — and **diverges by a
factor of 1.6 to 3.2 at the warm end**, far outside the ±25% digitization band those anchors
carry. The independent review's caution that the warm-side prism anchors sit one-sidedly low is
confirmed, and it is worse than the review estimated: not 25–40% low, but low by a factor of two
to three at −2…−5 °C. **Every warm-end result this project has produced rests on a σ₀_prism that
Libbrecht's own printed form puts 2–3× higher.**~~

> # ⚠ RETRACTED AS A HABIT ARGUMENT — function-level diagnostic only (2026-08-01)
>
> The census below counts crossings of the two broad-facet `sigma_0(T)` functions. It does
> **not** count habit transitions and cannot bound them. Habit is produced by the complete
> facet attachment coefficient `A(T)·exp(-sigma_0(T)/sigma_surf)`, with `sigma_surf` supplied
> by the coupled diffusion/boundary solve and changing over the growing crystal. In particular,
> the CAK prism prefactor is not generally one, and even equal `sigma_0` values do not require
> the basal and prism surfaces to see equal `sigma_surf`.
>
> The earlier text called this a “structural result,” claimed that the dip terms were what
> created the simulated habit transitions, and said one crossing settled the no-SDAK case.
> Those claims did not follow from the quantity computed and are withdrawn. The numeric rows are
> retained only as a reproducible description of the printed `sigma_0` functions; they carry no
> morphology verdict or causal attribution. See the correction at the head of
> [`../docs/decisions/0025-phase6-agreement-scoring-rule.md`](../docs/decisions/0025-phase6-agreement-scoring-rule.md).

**Crossing counts — retained `sigma_0(T)` diagnostic:**

| curves | crossings of σ₀,basal = σ₀,prism |
|---|---|
| **M2 broad-facet branch** | **one**, at −8.39 °C |
| registered CAK broad-facet `sigma_0` anchors | **one**, at −10.00 °C |
| **M1 (with SDAK dips)** | **three**: approximately −3.08, −8.07, −24.73 °C |

At the function level, the two sampled broad-facet `sigma_0` pairs each have one crossing, while
the printed M1 dipped functions have three. All three M1 crossings are near the three source-side
habit separators they were chosen to reproduce. That is in-sample construction, not a validation
statistic or a demonstrated route from the dip factors to morphology.

ADR 0005's in-sample classification still follows from the source's explicit statement that the
M1 dip locations were selected to reproduce the Nakaya diagram. This crossing census neither
strengthens nor weakens that provenance judgment. The only admissible habit evidence is a forward
solver result evaluated by the frozen morphology operator, with numerical controls satisfied.

## 4. The Kelly & Boyer correction (N3) — confirmed here, verbatim

> "[Brief technical aside: In the CA model, it is not necessary to 'invert' the function α(σ) as
> was suggested in [2014Kel]. Rather one can numerically relax the external supersaturation field
> while **simultaneously** relaxing the boundary values of σ and α(σ) at each point on the ice
> surface. In this way, only the direct function α(σ) is needed]."

The stretch register's attribution of this correction to 2306.13087 was recorded as unconfirmed;
it is now confirmed. `solver-cpu/src/lk-solver.ts` conforms on both counts — checked 2026-07-27
and recorded in the Phase 6 plan.

## 5. SDAK's own status, in the source's words

> "the SDAK phenomenon provides the only viable option currently available that can adequately
> explain the Nakaya diagram together with a plethora of other ice-growth data."

> "We still treat the SDAK phenomenon as a working hypothesis."

This is the framing guard for any Phase 6 report: a no-SDAK model failing to reproduce the diagram
is **Libbrecht's own published expectation**, not a discovery of ours. Phase 6 executes a
pre-registered test of named solver configurations; this source note makes no priority claim and
does not call an M1 comparison independent validation.
