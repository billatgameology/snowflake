# Evidence bytes are HASH-REGISTERED. Git must never rewrite them.
#
# Found 2026-08-01, immediately after `evidence/` was created (ADR 0038). The commit output said:
#
#   warning: LF will be replaced by CRLF in evidence/phase6-sweep/points.json
#
# This machine has `core.autocrlf=true`. The committed blob is correct — it is LF and its sha256 is
# the registered `0ed613bc…` — but autocrlf converts on CHECKOUT, so a fresh clone on Windows would
# write CRLF into the working tree and **every sha256 printed in the published reports would fail
# against the file the reader actually has.** The evidence would be intact in the object store and
# unverifiable in practice, which is the worst of both.
#
# `-text` disables all end-of-line conversion in both directions while keeping the files diffable
# (unlike `binary`, which would also suppress diffs on what are JSON and SVG).
#
# The rule is deliberately the whole subtree: an evidence file added later must not have to remember
# this.
evidence/** -text

# The manifests are the checking mechanism for the above, so they get the same treatment.
evidence/MANIFEST.json -text
evidence/OUT-TREES-MANIFEST.json -text

# This archive deliberately preserves a mixed-CRLF/LF historical buffer byte-for-byte. Without an
# exact-path override, this Windows host's core.autocrlf=true clean filter changes it while adding
# the file, so the committed blob would not match the recorded raw-body identity. Git's whitespace
# checker otherwise treats each preserved CR byte as trailing whitespace, so disable that one check
# only for this exact immutable archive path.
docs/progress-history-through-2026-08-02.md -text whitespace=-trailing-space

# The live source audit and exact rejected V4/V4.x history must survive Windows checkout byte-exact.
research/phase6-source-currency.md -text
docs/libbrecht-parameters.md -text
research/rejected/phase6-wp1-v4x/phase6-source-currency-v4x-rejected.md -text
research/rejected/phase6-wp1-v4x/phase6-wp1-independence-operands.json -text
research/rejected/phase6-wp1-v4x/phase6-wp1-review-findings.json -text
research/rejected/phase6-wp1-v4x/phase6-wp1-search-registry.json -text

# The two pinned WP1 size-strata inputs: their LF-normalized identities are published inside the
# hash-registered freeze artifact, so a fresh Windows checkout must reproduce those bytes directly
# (WP1 non-author review hardening 3, 2026-08-06).
research/phase6-heldout-candidate-lock.json -text
research/nakaya-morphology-diagram.md -text

# Phase 8 freezes raw-byte identities for the target book, its report, every cited extraction
# index, the extended historical dataset, and the registered derived-observable implementation.
# Keep those exact bytes reproducible on Windows checkouts as well as this macOS review host.
.gitattributes -text
research/phase8-target-book.jsonl -text
research/phase8-target-book.md -text
research/lab-validation-dataset.jsonl -text
research/libbrecht-papers-extracts.md -text
research/takahashi-fukuta-1988.md -text
research/takahashi-1991.md -text
research/nelson-1998.md -text
research/bailey-hallett-2002.md -text
research/bacon-baker-swanson-2003.md -text
research/harrison-2016.md -text
research/pokrifka-2020.md -text
research/harrington-pokrifka-2026.md -text
core/src/target-observables.ts -text
core/src/index.ts -text
core/test/target-observables.test.ts -text
docs/target-book-observables.md -text
runner/src/phase8-target-book.ts -text
runner/src/gate4-evidence.ts -text
runner/src/phase8-freeze.ts -text
runner/src/phase8-verify.ts -text
runner/test/phase8-target-book.test.ts -text

# Phase 8B's registered operators, terminal research records, successor plan, and implementation
# are hash-bound inputs to the final benchmark. Preserve their exact bytes on Windows checkouts.
research/phase8b-plot-operator-v1.json -text
research/phase8b-plot-operator-v2.json -text
research/phase8b-residual-audit-registration.md -text
research/phase8b-residual-audit-result.md -text
research/phase8b-targeted-gap-and-currency.md -text
docs/plans/phase-9-modular-physics-arms.md -text
runner/src/phase8-benchmark-selection.ts -text
runner/test/phase8-benchmark-selection.test.ts -text
runner/src/phase8-native-history.ts -text
runner/src/phase8-native-history-verify.ts -text
runner/test/phase8-native-history.test.ts -text
runner/src/phase8-plot-extraction.ts -text
runner/src/phase8-plot-extraction-verify.ts -text
runner/test/phase8-plot-extraction.test.ts -text
runner/src/phase8-benchmark-final.ts -text
runner/src/phase8-benchmark-final-verify.ts -text
runner/test/phase8-benchmark-final.test.ts -text
