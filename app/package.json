{
  "name": "@vcc/app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Phase 3 development visualization: Three.js instrument over the CPU oracle running in a Web Worker. A debugging instrument first, product feature second (charter §3.1, §3.2 Phase 3).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "visual": "node scripts/visual.mjs"
  },
  "dependencies": {
    "@vcc/core": "*",
    "@vcc/solver-cpu": "*",
    "@vcc/solver-gpu": "*",
    "mediabunny": "1.55.7",
    "three": "^0.185.1",
    "tweakpane": "^4.0.5"
  },
  "devDependencies": {
    "@tweakpane/core": "^2.0.5",
    "@types/three": "^0.185.1",
    "playwright": "^1.61.1",
    "vite": "^7.3.6"
  }
}
