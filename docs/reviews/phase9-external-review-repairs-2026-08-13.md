# Phase 9 external-review repairs — 2026-08-13

## Scope and provenance

This is the repository disposition of the maker-supplied
[different-model external review](phase9-external-review-2026-08-13.md). The repairs were authored
by OpenAI Codex with the original repository and development context. They do not constitute the
independent post-fix review; that review is recorded below after the final bytes are stable.

## Accepted repairs

1. S0B is described as an internally recorded in-session shelf freeze, and D-BT and M-F/M-K2 as
   internally frozen, reviewed in-session protocols. None of those pre-score timings is independently
   demonstrable from Git ordering because detailed S0B, protocol and result bytes first entered Git
   together in `1efe127`. Frozen evidence bytes were not rewritten. Future deciding protocols must
   enter Git before their outputs are inspected.
2. A new sibling parser suite executes valid native-mass, native-dimension/rim and plot-series
   paths plus named fail-closed mutations. It instruments a SHA-pinned in-memory copy so the
   launch-bound adapter source and original test remain byte-identical.
3. Earlier review records now state their same-agent or same-model independence limits. The
   external Claude/Fable 5 review supplies the different-model, no-shared-context check.
4. The external review record preserves its independently re-executed full-suite counts rather
   than retroactively attributing those counts to the original completion run.
5. [`phase9-publication-verification-v1`](../../evidence/phase9-publication-verification-v1/)
   preserves the clean-head commands, runtime, exact stdout, subject/verifier/launch identities,
   controls, verdicts and limits for both sibling verifier passes. The immutable producer reports
   remain candidates that do not self-attest; candidate bytes plus the exact-byte receipt establish
   the independently verified development result.
6. The maker guide now explains the omitted-fold naming limit, the condition-sibling advantage of
   the fitted D-BT rival, and the M-F comparisons with the zero-growth control.
7. The guide-review record retains the intermediate `cc228d49…e313ddc` audit hash beside the limit
   that its bytes were never preserved, rather than deleting historical provenance.
8. The external-review summary now retains the repaired state contradiction, v2 assurance-only
   failure, parser/Rule 6 severity and minor process findings. Its full maker-supplied text is a
   tracked transcription whose first 10,453 bytes reproduce the original attachment hash exactly.

The v1 D-BT candidate failed independent numeric serialization. The v2 scores, source diagnostics
and `report.result` were byte-identical to v3; v2 was rejected as an assurance candidate because a
named verifier control was vacuous, not because its scientific result differed.

## Final checks

- Exact [`TMPDIR=/private/tmp npm test` receipt](../../evidence/phase9-publication-verification-v1/post-review-suite-receipt.json):
  exit 0; Rule 7 clean over 965 files; both TypeScript projects passed; Vitest 116/116 files,
  1,924 passed and seven skipped (387.15 s). The full terminal output is preserved beside the receipt.
- D-BT sibling verifier receipt: exit 0, 57,199 raw rows, five reconstructed artifacts, all 15
  controls observed and rejected or distinguished.
- M-F/M-K2 sibling verifier receipt: exit 0, five reconstructed artifacts, all nine controls
  executed and rejected.
- Launch-bound adapter source and original test: unchanged at their registered SHA-256 identities.

## Scientific disposition and limits

No headline number, score, diagnostic label, promotion decision or validation boundary changed.
These repairs did not rerun a physical solver, revisit the 59-source visual audit, reassess source
rights or currency, or create held-out evidence. Parser tests are hermetic unit checks; they do not
turn unsupported-dimension or plot families into validated production consumers. The generic plot
parser is exercised through its planar branch; currently unused non-planar branches remain outside
this bounded repair, and no Phase 9 quantitative result depends on them.

## Independent post-fix review of the 2026-08-13 repair bytes

**Verdict: NO BLOCKER.** Three read-only non-author reviewers were OpenAI Codex, GPT-5 family,
with inherited repository and development context. Same-model-family independence is therefore not
established; the maker-supplied Claude/Fable 5 review remains the different-model,
no-shared-context audit.

The parser reviewer re-executed 12/12 focused tests, both typechecks and Rule 7, verified the
instrumentation was non-vacuous, and confirmed the two launch-bound files remained byte-identical.
The receipt/document reviewer freshly reran both sibling verifiers with byte-identical stdout,
re-executed the 26 focused parser/integrity/progress tests, checked 83 local links, Rule 7 over 964
files, all manifest/index identities and both diff checks. The overall reviewer independently
checked the complete repair disposition, suite receipt, claim boundaries and absence of unrelated
solver, app, core, research or `HANDOFF.md` changes. All returned no blocker.

Limits: the reviewers did not repeat the full 389-second suite after the receipt was added; that
round did not preserve its raw terminal stream. They did not visually re-audit all 59
sources, reassess rights or currency, rebuild the pipelines in another language, inspect unbound
NAS content or run a physical solver. Unused non-planar plot branches remain outside this repair.

Those limits describe that review round. The maker-directed record delta below subsequently reran
the suite and preserved its terminal output; it does not retroactively expand the earlier review.

## Maker-directed record-integrity delta — 2026-08-14

- S0B now carries the same Git-order caveat as the deciding protocols: its files record an
  in-session pre-score freeze, but detailed S0B and scored-result bytes first entered Git together
  in `1efe127`, so repository ordering cannot independently prove the sequence. The progress test
  enforces the caveat; frozen evidence remains untouched.
- The guide-review record again retains the intermediate `cc228d49…e313ddc` digest while stating
  that the reviewed bytes were never preserved.
- The external-review summary restores its repaired state/v2, parser/Rule 6 and minor process
  findings. A tracked transcription removes dependence on the session attachment cache.
- Exact `TMPDIR=/private/tmp npm test` passed on the delta, and the raw terminal output is now
  manifest-pinned with the receipt. No scientific result or claim boundary changed.

## Final delta review

**Verdict: NO BLOCKER.** The read-only non-author reviewer was OpenAI Codex, GPT-5 family,
with inherited repository/development context; same-model-family independence is not established,
and Claude/Fable 5 remains the different-model, no-shared-context review.

The reviewer independently confirmed the common `1efe127` S0B/protocol/result entry history and
every corrected chronology statement; confirmed the restored `cc228d49…e313ddc` historical hash
remains explicitly paired with its unpreserved-byte limit; proved the tracked review's first 10,453
bytes reproduce the maker attachment with only one final LF added; decoded and rehashed the 31,725-byte suite transcript; verified every receipt, index and
manifest identity; freshly reran both sibling verifiers with byte-identical stdout; passed the 26
focused parser/integrity/progress tests; checked 84 local links, Rule 7 over 966 current files and
both diff checks; and found no change under solver, app, core, research, charter, decisions,
`HANDOFF.md` or the frozen Phase 9 result bundles.

Limits: the reviewer verified the preserved full-suite bytes but did not rerun the 387-second suite.
The current Rule 7 count is one higher than the captured run because record-only bytes were added
after it. The review did not repeat the 59-source visual audit, rights/currency work, independent
pipeline reimplementations, unbound NAS inspection or a physical solver run. Git proves co-entry in
`1efe127`, not the claimed internal chronology.
