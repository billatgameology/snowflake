# Part 1 prepared film — structure, surfaces and branching

## Outcome and boundary

Website `explore/film-part1@834c7690cc46ec9da8cfae09344ce9814597e1f4` extends the prepared
film through S06/S11/S09/S13. The [receipt](../video/part1-prepared-verification.json) derives
1040 seconds (17:20), 40 reviewed rows, 1930 spoken words and 113 provisional captions from
the imported [score](../video/part1-prepared-score.json), authority revision `a2c5555`.
The default local `/film/part-1` plays this prepared material; `/film/part-1/opening` retains
the original opening and `/film/part-1/prototype` retains the short study. No later unproduced
rows are activated. Nothing was pushed, merged, published or uploaded.

This is a produced draft with timing tones, not the complete film, recorded narration, or
maker visual acceptance. The unchanged script still needs the maker's aloud read and recording.

## What changed

- Added original cue-time diagrams for historical packing/diffraction questions, the ideal ice
  Ih oxygen sublattice, basal/prism faces, lattice limits, rough/smooth terraces, slow-face
  survival by addition, delivery/attachment, wandering molecules, depletion contours, the
  substrate-droplet threshold principle, and corner feedback. These do not compute new physics.
- Reused the bound Chapter 3 oxygen basis and its nearest-neighbor construction. Hydrogen
  placement is explicitly omitted. The molecular lattice, macroscopic outline and Run B model
  cells remain distinct. The rotating view is not a molecular-growth simulation.
- The slow-face illustration uses outward-moving support planes: the initial solid remains
  inside later outlines. Contours describe qualitative concentration levels, not a measured
  Run B field. Inward delivery and outward growth have separate labels.
- The model stays offstage after the seed and explicitly cuts to a cropped mid-growth branch.
  The original opening is copied unchanged except its endpoint visual becomes a transition in
  the extended edition. Neither the complete final model nor a new physical-time claim appears.
- Centralized edition identities across routes, static pages, import and capture. All editions
  have independent audio durations, score digests and model-reading images; all are reachable
  without JavaScript. The prepared artifact can grow without creating another edition per slice.
- Made new phone figures portrait-aware, including a larger oxygen lattice and stacked rough/
  smooth surfaces, with reflowing qualifications and no duplicated heading. Preserved the
  established browser-rendered site and scroll/read transport; no AI raster assets were needed.

## Visual observations, defects and repairs

Root inspected new-family desktop exports and actual lattice playback in the live browser,
then phone/200-percent text captures. This was sampled viewing, not an uninterrupted watch of
the entire film. The early lattice label crossed the drawing; it now clears the lattice. The
viewing-angle gauge replaces a misleading fixed upward c-axis arrow. Small comparison atoms
scale with their inset. The initial-front ghost now matches the actual unperturbed ellipse.

The initial model return showed nearly all the mid-growth arms. Tightening only the selected
camera keys restored branch emphasis. The delivery/attachment shot now follows material toward
one surface, rather than suggesting two unrelated incoming flows. Phone landscape miniatures
and duplicated titles were replaced with larger reading compositions. These are visual/editorial
judgments, not scientific evidence or a claim that final narrated pacing is accepted.

The non-author `science_review` agent, using shared thread context, read the bound next-sequence
sources and inspected a bounded set of root-generated PNGs. It caught a real mismatch: radial
color shading and bump-shaped contours implied different concentration fields. The fix removes
the radial wash and orders the deformed contours on neutral air. Its final source-only check
confirmed that repair, the matching initial ellipse and the portrait geometry. Root inspected
the final phone crops/label association. This was not a fresh audit of the entire narration.

The read-only `runtime_review` agent caught a shadowed edition variable that would request an
undefined caption file during export, and the missing static link to the retained opening.
Both were repaired. It also reviewed the late-metadata fix and proposed the executed delayed-
response test. It did not edit the website or run browser checks. `story_review` authored the
bounded compiler/artifact and camera adjustment in this repository; root reviewed and imported
them. Root alone edited the website and operated its browser/build/capture tools.

## Playback failures that the visual pass exposed

An early phone seek could arrive before the longer timing track's metadata. The visual changed
but the media position did not. The metadata handler now applies the latest controller position
without autoplay. A held-response test seeks twice before releasing audio and verifies that the
newest time survives, remains paused and subsequently plays from that position.

Early phone smoke assertions checked advancement but missed a reset caused by focusing Pause:
automatic control scrolling was interpreted as reading the first article row. Root noticed the
wrong scene in the actual screenshot. The runtime now distinguishes control-focus scrolling
from explicit reader input, and mapping to narration begins at the article. Desktop watch mode
also leaves the old sticky-reader document position. Tests now assert the cue after Pause and
after enlarged-text reflow, not just that the audio advanced at some earlier instant.

The prototype wheel test initially observed an earlier programmatic return-to-watch scroll.
Its recorder now arms on the actual tested wheel, while retaining the requirement that audio
is paused before the ensuing native scroll. No screenshot tolerance or scientific gate was relaxed.

## Verification and limits

The [receipt](../video/part1-prepared-verification.json) records source/report identities and actual
command output: 21 focused tests, successful TypeScript/Vite build, 9 prepared browser checks,
8 opening checks and 18 prototype checks. The build retains its existing large-chunk warning.
All three generated static pages have the expected scene counts and available model images.

The final prepared export contains 59 sampled still entries and passes exact repeated/reverse/
reload comparison on the named software-2D-raster/Metal-WebGL capture recipe. Its decoded sample
is 6.000000 seconds, 180 frames, 1920×1080 at 30 fps, with 48-kHz audio. Sample export wall time
is 21.811 seconds. All recorded implementation hashes match the committed website. The retained
opening/prototype still exports also pass. An actual wrong-edition capture is rejected before
any still is written. These numbers are copied from the receipt, not extrapolated to a full film.

Source/prose checks: `node scripts/build-part1-prepared-score.mjs --check`, the opening and full
production compiler checks, `node scripts/lint-rule7.mjs`, and `git diff --check`. No scientific
`npm test`, phase gate, solver, scientific readout, evidence publication or chapter changed.
Browser phone emulation is not real-device or screen-reader certification. Whole-film timing,
continuous playback/export cost, voice-aligned captions and release remain unverified.

Next: S15, “The shape remembers,” in `docs/video/part1-script.md`. Record its bounded execution
selection in the active plan, extend the same prepared score, and build the growth-history story
without replaying the full crystal reveal. No further scene-plan review is needed to continue.
