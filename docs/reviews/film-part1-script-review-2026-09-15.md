# Part 1 complete-script scientific/source review — 2026-09-15

## Verdict

**Resolved for the reviewed text: no recording-blocking scientific or source-fidelity issue
found.** This is the complete WP2 non-author text review, not another plan review. No further
dialogue correction is required by this review. Final recording approval remains pending the
maker's aloud read and any resulting revisions; this review does not accept an unbuilt film.

Reviewed artifact: [`docs/video/part1-script.md`](../video/part1-script.md), all sixteen sequences,
VOICEOVER and ON SCREEN columns, bindings, production contract, credits and handoff. Its SHA-256
at review completion was
`608c4cb6e6406f996a265d0f0ca624d535c97d6b9ce221cbe959b9f3ec43a243`.
Line references below refer to those bytes. The file was untracked; checkout HEAD at the final
independent mechanical check was `40e7891636a91f3d6dc054f8cc8b1782db526028`.

## Reviewer and independence

- Reviewer: Codex/OpenAI, separate `science_review` agent; inherited session model, without a
  reviewer model override. The exact served model identifier/build is not exposed to this agent.
- The full script was written by a different agent. I did not author or edit this full draft.
  I shared the coordinating conversation, earlier plan findings and repair context, and had
  previously supplied a short source-checked halo/branching prototype narration. This is
  non-author review of the complete draft, not a context-blind or different-model review claim.
- I read the complete script and the cited local chapter passages, the JTS-M004 recollection,
  and the earlier plan review's author-response addendum. I compared the load-bearing claims
  with their actual passages, rather than accepting the author's pending-review table as proof.
  Existing charter/model distinctions and parameter-specification limits also govern the review.
- I independently executed the source/anchor, timing, word-count and file-identity checks below.
  No solver, evidence gate, external literature search or finished-film evaluation was run.

## Scientific/source disposition

The source root in this table is `docs/education/chapters/`. All chapter bindings remain pinned
to `929189b7ff0132fe288a562796812c68ec886840`, as declared in script lines 14–16. The table records
the decisive comparisons; it does not replace the script's full source bindings.

| Script location | Checked against | Disposition |
|---|---|---|
| S00, lines 48–61 | `docs/journey/TRANSCRIPT.md#jts-m004--the-selected-origin-recollection`; `09-the-menagerie.html#one-rule`; `01-not-a-frozen-raindrop.html#no-blueprint` | The toy, laboratory-growth viewing, few-rules impression and “Easy peasy” are supported personal paraphrase. It does not claim a physical laboratory visit or identify the uncertain video. Nineteen cells are explicitly model cells, not molecules. |
| S01, lines 66–79 | `07-plates-columns-plates-columns.html#the-map`, `#alternation`, `#cold-end-rewritten`, `#mystery`; `08-a-snowflake-is-a-record.html#a-map-for-still-weather` | Representative warm bands, supersaturation and constant-condition scope are retained. The cold-end account is qualified; no precise switch point, universal map, or present-day “nobody knows” claim is introduced. |
| S02, lines 84–96 | `01-not-a-frozen-raindrop.html#two-words`, `#not-a-raindrop`, `#into-the-cloud`, `#supercooling` | Deposition, seed formation, aggregation and rime are distinct. “None freeze right at zero” is scoped to the described droplets and matches the cited passage; the animation explicitly allows freezing below zero. The S04 author-response qualification is honored. |
| S05, lines 101–114 | `01-not-a-frozen-raindrop.html#the-relay`, `#too-cold-to-snow`; `04-the-fuel-supply.html#what-saturated-means`, `#the-gap` | Equilibrium over ice versus supercooled liquid is correct in the stated setting. The roughly hundred-thousand-droplet figure is attributed and limited to a large stellar crystal. Fuel exhaustion describes the selected cloud history, not a global cold-temperature prohibition. The S05 author-response qualification is honored. |
| S06, lines 119–133 | `02-four-hundred-years-of-looking.html#kepler`, `#x-rays`; `03-why-six-sides.html#the-honeycomb`, `#naming-the-faces`, `#what-it-does-not-say` | The historical claim is limited to Kepler's question and later structural investigation. Ice Ih stacking, equivalent directions and face names are distinguished from habit and detailed arm matching. No bond-angle-only derivation or unsupported historical priority survives. |
| S11, lines 138–150 | `05-the-restless-surface.html#the-slowest-faces-win`, `#the-attachment-coefficient`, `#the-low-energy-myth` | Slow-face selection is growth by addition. The Hertz–Knudsen attachment coefficient remains a response relative to the kinetic limit, not the unrelated G-G threshold. Delivery and attachment are explanatory constraints, not an exhaustive dismissal of heat transport. |
| S09, lines 155–168 | `04-the-fuel-supply.html#the-last-millimetre`, `#two-bottlenecks-in-series`, `#the-hungry-halo`; `05-the-restless-surface.html#how-a-facet-stays-flat` | Net diffusion follows concentration gradients; flux is not equated with local concentration. Field relaxation is scoped to slow growth. The droplet-free threshold is an experimental clue explained by an original diagram, not a complete measured field or Run B output. |
| S13, lines 173–187 | `06-the-runaway-bump.html#the-runaway-bump`, `#six-corners-at-once`, `#turn-off-the-directions`, `#order-and-chaos`; Chapter 1 Fig. 1.9 caption | The perturbation argument is introduced as a simple setup, not every flat facet. Directional-control evidence is model work. Run B is explicitly separated from the physical argument and cannot validate that argument by resemblance. |
| S15, lines 192–207 | `08-a-snowflake-is-a-record.html#a-map-for-still-weather`, `#reading-a-capped-column`, `#why-the-six-arms-match`, `#where-the-symmetry-runs-out`, `#what-the-record-does-not-say` | Selected deposition history, column-first capped-column growth, shared environmental response and fine-scale differences are supported. Nonunique history and continued field coupling remain explicit; no blanket preservation theorem is asserted. |
| S17, lines 212–226 | `02-four-hundred-years-of-looking.html#nakaya`; `07-plates-columns-plates-columns.html#the-rule`, `#not-diffusion`, `#second-opinions`, `#cold-end-rewritten` | The thin-plate hook is immediately scoped to comparable end/side attachment and sustained extreme aspect ratio. Thickening is addition, and branching can continue. This follows the S18 author-response addendum without making a universal theorem about diffusion and every plate-like shape. |
| S22, lines 231–244 | `10-how-we-know.html#two-thermometers` (Eq. 6.1), `#empty-substrate`, `#counting-fringes` | The ideal excess is normalized by the substrate saturation value. Ideal zero is separated from experimental zero and uncertainty. Neighbor consumption, inference and substrate effects survive. Laser cycling and white-light spectral bands are correctly distinguished. |
| S25, lines 249–261 | `11-the-stickiness-of-ice.html#starting-a-layer`; `05-the-restless-surface.html#a-staircase-not-a-wall` | Terrace nucleation is explicitly defect-free; mobile surface molecules are not automatically incorporated ice. The screw-dislocation alternative prevents an all-surfaces claim. Island sizes and waiting times remain schematic. |
| S26, lines 266–278 | `11-the-stickiness-of-ice.html#the-barrier`, `#where-the-numbers-come-from`; `13-the-frontier.html#four-kinds-of-line` | Observed speed, estimated surface supply, inferred attachment response and fitted parameters remain separate. Broad-facet scope is retained. No numerical low-pressure result is silently generalized into a universal value or directly measured barrier. |
| S28, lines 283–297 | `12-why-the-shape-flips.html#narrow-facets`, `#esi`, `#how-sure`; `13-the-frontier.html#working-hypothesis` | The narrow-facet mechanism is a hypothesis before it moves. Surface effects, gas diffusion and edge feedback stay coupled. Growth evidence, model-inferred barrier reductions and chosen analytic dip forms are distinct. The incomplete quantitative account is expressly scoped to the reviewed sources. |
| S32, lines 302–316 | `13-the-frontier.html#where-it-runs-out`, `#working-hypothesis`, `#why-bother`; `docs/libbrecht-parameters.md`; charter §§1.3, 1.5, 2.6–2.7 | The conclusion distinguishes established machinery, inference, fits and hypotheses. Heat and changing transport require assessment; fitted influence is not the same as explicitly implementing a process. Naming an omission is not represented as bounding its error. |
| S34 and credits, lines 321–342 | Earlier physical passages; `13-the-frontier.html#why-bother`; `08-a-snowflake-is-a-record.html#what-the-record-does-not-say`; JTS-M004 | The physical recap is explicitly on a diagram before the Run B reveal. Model beauty is not validation. The personal return is supported; future comparison is a proposed next task, not a fabricated outcome. No source quotations or attribution problem requiring correction was found. |

## Keep these conditions in production

These are already present in the draft, not new text blockers or additional review gates:

1. **S00/S13/S34 and lines 33–35:** verify the selected Run B asset against its named record,
   including the nineteen-cell initial state, before recording asset-specific copy. I did not
   decode that replay or inspect its full asset chain in this script review. Keep MODEL ·
   UNVALIDATED visible, and do not add temperature, physical-time or molecular-scale meaning.
2. **S09/S13, lines 160–187:** keep the depletion/bump visualization qualitative. The source's
   simplified diffusion-limited front is not an arbitrary stable facet; a caption such as
   “simplified diffusion-limited front” would make the existing setup especially legible.
   Do not infer a quantitative field or causal measurement from a decorative drawing.
3. **S22/S26/S28, lines 235–297:** retain the measurement/inference/fit/hypothesis labels in
   rendered frames, captions and descriptive transcript. Any later numerical axis, dataset
   point, molecular width or quantitative comparison needs its own exact source/check.
4. **Lines 23–29 and 381–384:** time the maker's actual read and derive captions from the actual
   take. The arithmetic below is not evidence of comfortable delivery or a finished duration.

## Independently executed checks

Executed a read-only `node --input-type=module` check against the reviewed file using `node:fs`,
`node:path`, `node:crypto` and `git show` via `node:child_process`. It:

- SHA-256-hashed the complete script and checked each local Markdown link's file existence.
- Checked all 58 cited HTML-anchor occurrences against literal `id` values: no missing anchor.
  The JTS-M004 and author-response Markdown headings were also read and matched manually.
- Compared each of the thirteen linked chapter files byte-for-byte with
  `git show 929189b7ff0132fe288a562796812c68ec886840:<path>`: all matched.
- Parsed every timed table row into TIME, VOICEOVER and ON SCREEN. Counted only VOICEOVER
  whitespace tokens containing a Unicode letter or number, excluding em-dash silent rows.
  Independently reproduced **3,924 words, 64 spoken rows, 1,920 spoken seconds, 200 silent
  seconds, 2,120 total seconds (35:20), maximum allocated rate 140 words/minute**. Found no
  gaps, overlaps or malformed timed rows. These values were copied from that check's output at
  report-write time, not inherited from the writer's prose; the artifact is the hashed script.

No solver/phase/evidence suite was necessary for this read-only source review and prose report.
`node scripts/lint-rule7.mjs` exited 0, reporting `rule7: clean`. For this new, untracked report,
`git diff --no-index --check /dev/null docs/reviews/film-part1-script-review-2026-09-15.md`
emitted no whitespace diagnostics; its exit 1 reports that the two compared files differ.

## Closure limits and next action

| Item | Disposition |
|---|---|
| Complete-script scientific/source text review under Rules 10/13 | Resolved for the hashed draft; no open correction request. |
| All scientific literature and every quotation in the education chapters | Not re-audited wholesale. This checks selected script claims against the bound local passages and their recorded qualifications, not the entire research field as of release day. |
| Final model-asset provenance, rendered visual truth, access behavior, audio/caption synchronization, maker delivery and sub-hour finished export | Pending production work; not proven by this report. |
| Changed dialogue or newly quantitative visuals after this review | This report does not silently cover changed content. Check the affected claims and recompute mechanical totals when applicable. |

Next: the coordinator may link this report and record WP2 text-review completion, then proceed
to the maker's table read and the existing production checks. Do not label final recording or
finished-film acceptance complete on the strength of this text review alone.

## Addendum — selected Run B seed and identity resolved, 2026-09-15

**The selected-asset seed/provenance condition above is now resolved for these bytes.** I read
the website decoder's length-prefixed JSON/interleaved little-endian uint32 contract in
`/Users/clipper/github/snowcrystal_website-film-part1/src/growth/growthAsset.ts`, the source plan's
“Full Run B bake — complete and independently validated” record, and the canonical lattice seed
definition. I then independently parsed the actual website payload without calling its decoder.

The executed command below exited 0 from `/Users/clipper/github/snowflake`. Its measured output
was: **7,695,060 bytes**, a **2,280-byte header**, **961,597 unique tick/index-ordered events**,
minimum tick **0**, maximum tick **70,000**, and first post-seed attachment tick **65**. Exactly
**19** payload events occur at tick zero. Their complete index set equals the independently
enumerated radius-2, thickness-1 axial seed centered at **[600, 600, 24]**; this is not merely a
read of the header's `seedCount` assertion. S00's “It began with nineteen cells” is supported.

The file SHA-256 is
`475c1f7c227c45b005bfdb8691b1250599b405fa59902462110312a4f26ceb7d`, matching both
[`part1-score.json`](../video/part1-score.json)'s selected model and the
[Run B source record](../plans/explore-gutcheck-growth-volume.md#full-run-b-bake--complete-and-independently-validated).
Its embedded provenance names source commit `44fd4b604cddf1c1c30ed4aec2afba5bdc18be5c`,
Node `v24.13.1`, V8 `13.6.233.17-node.40`, and the 701-frame legacy manifest digest
`a06bf93000ab948cd72617649530bcf96d39dbcd01badb22d5617a1b73d17c4d`, consistent with that record.

```sh
node --input-type=module <<'NODE'
import fs from 'node:fs';
import crypto from 'node:crypto';
import assert from 'node:assert/strict';
const file='/Users/clipper/github/snowcrystal_website-film-part1/public/growth/run-b-growth-v1.bin';
const b=fs.readFileSync(file), n=b.readUInt32LE(0), h=JSON.parse(b.subarray(4,4+n));
const score=JSON.parse(fs.readFileSync('docs/video/part1-score.json','utf8'));
const plan=fs.readFileSync('docs/plans/explore-gutcheck-growth-volume.md','utf8');
const sha256=crypto.createHash('sha256').update(b).digest('hex');
assert.equal(sha256,score.model.sha256); assert.equal(b.length,score.model.bytes);
assert(plan.includes(sha256)); assert.equal(h.format,'gutcheck-growth-v1');
assert.equal(b.length,4+n+8*h.eventCount); assert.equal(h.attachedCount,h.eventCount);
const {nx,ny,nz}=h.config.dims, center=[Math.floor(nx/2),Math.floor(ny/2),Math.floor(nz/2)];
const seen=new Set(), zero=[];let minTick=Infinity,maxTick=-Infinity,nextTick=Infinity,lastTick=-1,lastIndex=-1;
for(let p=4+n;p<b.length;p+=8){
 const index=b.readUInt32LE(p),tick=b.readUInt32LE(p+4);
 assert(index<nx*ny*nz);assert(!seen.has(index));seen.add(index);
 assert(tick>lastTick||(tick===lastTick&&index>lastIndex));lastTick=tick;lastIndex=index;
 minTick=Math.min(minTick,tick);maxTick=Math.max(maxTick,tick);
 if(tick===0)zero.push(index);else nextTick=Math.min(nextTick,tick);
}
const expected=[];
for(let dj=-2;dj<=2;dj++)for(let di=-2;di<=2;di++){
 if(Math.max(Math.abs(di),Math.abs(dj),Math.abs(di+dj))<=2)
  expected.push((center[2]*ny+center[1]+dj)*nx+center[0]+di);
}
expected.sort((a,b)=>a-b);assert.deepEqual(zero,expected);assert.equal(h.seedCount,zero.length);
assert.equal(h.config.seedRadius,2);assert.equal(h.config.seedThickness,1);assert.deepEqual(h.config.center,center);
assert.equal(maxTick,h.finalTick);assert.equal(h.source.git.head,'44fd4b604cddf1c1c30ed4aec2afba5bdc18be5c');
assert.equal(h.source.runtime.node,'v24.13.1');assert.equal(h.source.runtime.v8,'13.6.233.17-node.40');
assert(plan.includes(h.source.legacyComparison.sha256));
console.log(JSON.stringify({file,bytes:b.length,sha256,headerBytes:n,eventCount:seen.size,minTick,maxTick,firstPostSeedTick:nextTick,seedCount:zero.length,center,canonicalRadius:2,canonicalThickness:1,seedIndices:zero,sourceGit:h.source.git.head,sourceRuntime:h.source.runtime,sourceLegacyManifestSha256:h.source.legacyComparison.sha256,sourceLegacyFrameCount:h.source.legacyComparison.frameCount,scoreIdentityMatch:true,canonicalSeedExactMatch:true},null,2));
NODE
```

Limits: this closes actual payload identity and the seed assertion, superseding those pending
items in the earlier production list. It does not repeat the historical solver run, independently
reopen the NAS legacy manifest/checkpoint, or prove physical validity. Provenance fields are
compared with the named source record; historical execution is not inferred solely from their
presence. No website, script, score or progress file was changed. Visual rendering, delivery,
captions and the finished-film checks remain outside this addendum.

### Metadata-only script reaffirmation

The coordinator subsequently updated the script's review-status/handoff prose, not its film
content. I read those changes: they accurately link this resolved text review while retaining
the maker-read, affected-claim and final-recording limits. The original hash and line-reference
scope above remain the historical review record.

I independently checked the current bytes with read-only `node --input-type=module`. Reversing
only the five stated metadata transformations in memory recovered the original full-file
SHA-256 `608c4cb6e6406f996a265d0f0ca624d535c97d6b9ce221cbe959b9f3ec43a243` exactly. Direct comparison
then confirmed the entire substring from `## S00` through immediately before
`## Fact-check and production handoff` unchanged, including source bindings, all timed cells and
credits. Its SHA-256 was `636bc6a77f111e4b2692f78cc1de98fdc44e9ebed184fba6437de07ef8d0af20`.
The 82 timed rows, including silent rows, also matched byte-for-byte; SHA-256 of the UTF-8
`JSON.stringify` array of those row strings was
`4e406c36d55ec067ec88d062f6fbe77ca5bea4f5249dfb2319048fea69734c88`.
This agrees with the coordinator's separately preserved pre-edit row fingerprint, but the
recovered original-file hash provides an independent check of the stated metadata-only scope.

**The no-blocker scientific/source verdict is reaffirmed for the current draft below.** The seed
addendum resolves the selected asset check; the remaining maker-read and finished-film limits
still apply. Post-addendum `node scripts/lint-rule7.mjs` exited 0; the untracked-report
`git diff --no-index --check /dev/null docs/reviews/film-part1-script-review-2026-09-15.md`
emitted no whitespace diagnostics (exit 1 for differing files).

Current reviewed script SHA-256: `3a55b48ade327a360ce14d14dc2f7979bd956200c57120324bfdab2561b86e36`
