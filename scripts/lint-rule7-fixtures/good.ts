// Fixture: MUST pass the Rule 7 lint — every occurrence carries provenance,
// or is waived as a deliberate mention.
export const alphaHKBasal = 0.1;
export const ggThreshBeta = new Float64Array(8);
export const reiterAlpha = 1;
export const alphabet = "abc"; // natural word, lowercase continuation
// A deliberate mention: the banned spelling is alpha. rule7-waive: fixture demonstrating the waiver.
